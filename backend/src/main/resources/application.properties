# Server

server.port=${PORT:8080}
# Database - Change these to your MySQL credentials
#spring.datasource.url=jdbc:mysql://localhost:3306/chatapp_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
#spring.datasource.username=admin
#spring.datasource.password=admin
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver





# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Secret (change this in production!)
jwt.secret=chatapp_super_secret_key_change_this_in_production_2024
jwt.expiration=86400000

# CORS
app.cors.allowed-origins=http://localhost:3000
